# Основная библиотека Discord — версия с поддержкой slash-команд и UI
discord.py>=2.4.0

# Асинхронный драйвер SQLite (встроен в Python, обёртка для async)
aiosqlite>=0.20.0

# Загрузка переменных окружения из .env файла
python-dotenv>=1.0.0

# Асинхронный HTTP-клиент (используется discord.py внутри)
aiohttp>=3.9.0
